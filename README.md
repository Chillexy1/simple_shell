This is a simple UNIX command interpreter  WINMINGLE Project.

Every students are eexpected to learn and known without the use of google :

          .Who designed and implemented the original Unix operating system
          ·Who wrote the first version of the UNIX shell
          ·Who invented the B programming language (the direct predecessor to the C programming language)
          ·Who is Ken Thompson
          ·How does a shell work
          ·What is a pid and a ppid
          ·How to manipulate the environment of the current process
          ·What is the difference between a function and a system call
          ·How to create processes
          ·What are the three prototypes of main
          ·How does the shell use the PATH to find the programs
          ·How to execute another program with the execve system call
          ·How to suspend the execution of a process until one of its children terminates
          ·What is EOF / “end-of-file”?

REQUIREMENT:
    All files to be compiled on Ubuntu using gcc, using the options -Wall -Werror -Wextra -pedantic -std=gnu89

    All files must end with a new line

    no more than 5 functions per file

    All header files should be include guarded

    Use system calls only when you need to

    There should be no memory leaks in our shell
